version: '3.8'

services:
  web:
    build: .
    container_name: food_bridge_web
    ports:
      - "5001:5000"
    volumes:
      - .:/app
      - ./instance:/app/instance
    environment:
      - FLASK_APP=app.py
      - FLASK_DEBUG=1
      - FLASK_ENV=development
